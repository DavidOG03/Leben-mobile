# Leben - Guest Onboarding Flow
# Tests: App launch → continue as guest → dashboard visible
appId: com.leben.mobile
---
- launchApp
- assertVisible: "Continue as Guest"
- tapOn: "Continue as Guest"
- assertVisible:
    text: "Good"
    enabled: true
